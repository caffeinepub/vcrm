{
  "frontend/src/pages/SolarProjectDetailPage.tsx": "Simplified solar project detail placeholder page using the correct `from: '/layout/solar-projects/$id'` route ID for `useParams`, navigating back to `/solar-projects`.",
  "frontend/src/pages/AdminPage.tsx": "Admin panel with tabs for pending/approved/rejected users, approve/reject actions, and stats cards — uses `useGetCallerUserRole` (correct name) and navigates to `/dashboard` (correct registered route path).",
  "frontend/src/pages/AccessDeniedPage.tsx": "Simple access denied page shown when unauthorized users attempt to access restricted routes.",
  "frontend/src/components/StaticMapWidget.tsx": "Visual map widget rendering a grid-based coordinate display with a pin marker for customer lat/lng coordinates, no external APIs used.",
  "frontend/src/components/DeleteConfirmationDialog.tsx": "Reusable AlertDialog component for confirming delete operations with entity name, confirm/cancel callbacks, and loading state.",
  "frontend/src/components/LeadFormDialog.tsx": "Dialog form for creating and editing leads with name, contact, status, and notes fields; uses useAddLead/useUpdateLead mutations with inline loading spinner and toast notifications on success/error.",
  "frontend/src/components/CustomerFormDialog.tsx": "Dialog form for creating and editing customers with name, email, phone, address, latitude, and longitude fields.",
  "frontend/src/components/DealFormDialog.tsx": "Dialog form for creating and editing deals with title, value, lead selector (from real backend leads), and stage fields; uses useAddDeal/useUpdateDeal mutations with inline loading spinner and toast notifications.",
  "frontend/src/components/DealCard.tsx": "Compact deal card showing title, customer name, and value with a dropdown menu for editing, deleting, and moving to other pipeline stages.",
  "frontend/src/components/ReminderFormDialog.tsx": "Dialog form for creating and editing reminders with datetime-local input and note textarea, converting between bigint nanoseconds and JS dates.",
  "frontend/src/components/SolarProjectFormDialog.tsx": "Dialog form for creating and editing solar projects with customer selection, system size (kW), installation status, and site survey section (date, surveyor, notes).",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page that fetches real leads and deals from the backend via React Query hooks, displays stat cards (total leads, deals, won deals, revenue) and bar/pie charts — all starting at 0 with empty-state messages when no data exists.",
  "frontend/src/components/Layout.tsx": "Main authenticated layout with a purple sidebar (`#4B0082` background) using explicit `text-white` classes on all nav items and icons to ensure visibility; removes \"Refer & Earn\" from navigation; includes mobile hamburger menu and profile setup modal logic.",
  "frontend/src/pages/LoginPage.tsx": "Two-step OTP login page that stores both authentication state (`vcrm_otp_authenticated`) and the logged-in email (`vcrm_logged_in_email`) in localStorage after successful OTP verification, enabling the super admin email check in Layout.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions — covers leads CRUD, deals CRUD, deal stage moves, user profile, OTP auth, admin/approval operations, and role management.",
  "frontend/src/index.css": "Global styles with OKLCH color system — updated from deep-blue to emerald green and dark teal palette. Primary color is now emerald green (~155° hue), sidebar uses dark teal (~175° hue), orange accent highlights are preserved, and both light and dark modes use the new green/teal tokens.",
  "frontend/tailwind.config.js": "Tailwind configuration — updated color tokens to replace navy/blue-mid with teal/green-mid matching the new emerald green and dark teal theme. Box shadow colors updated to match the green palette. All other configuration unchanged.",
  "frontend/src/components/ProfileSetupModal.tsx": "Profile setup modal for new users; pre-fills the email field from `vcrm_logged_in_email` in localStorage, improves the auth error message to say \"sign out and sign in again\" instead of \"session expired\", and keeps all existing validation and error-categorization logic intact.",
  "frontend/src/pages/ProfilePage.tsx": "Professional profile page at /profile displaying user name, email, phone, role, approval status, and principal ID in a card-based layout with inline editing for name/email/phone, read-only role/status fields, and a Re-verify via OTP modal dialog with countdown timer and OTP input.",
  "frontend/src/App.tsx": "Main app router with TanStack Router — registers all routes including the new CRM routes (contacts, pipeline, pipeline/lead-summary, followup/$id, templates, reports) alongside legacy routes, with the router type registered globally so all navigate() calls type-check correctly.",
  "frontend/src/components/RoleAssignmentOTPDialog.tsx": "New dialog component for OTP-gated role assignment — presents a two-step flow (email input → 6-digit OTP with countdown timer and resend option), verifies OTP via backend, then calls `requestRoleAssignment` on success; shows appropriate error toasts on failure and matches the existing OTP UI patterns.",
  "frontend/src/pages/CustomerDetailPage.tsx": "Simplified customer detail placeholder page using the correct `from: '/layout/customers/$id'` route ID for `useParams`, navigating back to `/customers`.",
  "frontend/src/pages/LeadSummaryReportPage.tsx": "Lead summary report page rewritten to use real `useGetAllLeads` hook and `Lead` type fields (`name`, `contact`, `status`, `notes`) instead of removed VcrmLead fields.",
  "frontend/src/pages/ContactsPage.tsx": "Contacts page showing all leads fetched from the backend in a card grid with search, add/edit (LeadFormDialog), and delete (DeleteConfirmationDialog) functionality fully wired to backend mutations with toast notifications.",
  "frontend/src/pages/DealsPage.tsx": "Pipeline/Deals page showing a 4-column Kanban board (New, In Progress, Won, Lost) populated entirely from real backend data; supports adding, editing, deleting, and moving deals between stages with toast notifications.",
  "frontend/src/pages/CustomersPage.tsx": "Local-state customers page using correct route paths `/customers/$id` for navigation — no longer depends on removed backend hooks.",
  "frontend/src/pages/SolarProjectsPage.tsx": "Local-state solar projects page using correct route paths `/solar-projects/$id` for navigation — no longer depends on removed backend hooks.",
  "frontend/src/pages/FollowupDetailsPage.tsx": "Followup details page rewritten to use real `useGetAllLeads` hook and display lead data (`name`, `contact`, `status`, `notes`) instead of removed FollowupEntry type fields."
}