{
  "frontend/src/pages/SolarProjectDetailPage.tsx": "Solar project detail page showing system info, site survey details, and customer location map — fixed `useParams` `from` path to `/layout/solar-projects/$id` matching the actual TanStack Router route ID generated by the named layout route.",
  "frontend/src/pages/AdminPage.tsx": "Full admin panel page with tabs for Pending/Approved/Rejected users, Approve/Reject action buttons per user, user profile display via `useGetUserProfile`, stats cards, and an access guard that blocks non-admin users.",
  "frontend/src/pages/AccessDeniedPage.tsx": "Simple access denied page shown when unauthorized users attempt to access restricted routes.",
  "frontend/src/components/StaticMapWidget.tsx": "Visual map widget rendering a grid-based coordinate display with a pin marker for customer lat/lng coordinates, no external APIs used.",
  "frontend/src/components/DeleteConfirmationDialog.tsx": "Reusable AlertDialog component for confirming delete operations with entity name, confirm/cancel callbacks, and loading state.",
  "frontend/src/components/LeadFormDialog.tsx": "Dialog form for creating and editing leads with name, contact, status dropdown, and notes fields.",
  "frontend/src/components/CustomerFormDialog.tsx": "Dialog form for creating and editing customers with name, email, phone, address, latitude, and longitude fields.",
  "frontend/src/components/DealFormDialog.tsx": "Dialog form for creating and editing deals with title, value, customer dropdown, and stage selection.",
  "frontend/src/components/DealCard.tsx": "Compact deal card component displaying title, customer name, and value with a dropdown menu for stage moves, edit, and delete actions.",
  "frontend/src/components/ReminderFormDialog.tsx": "Dialog form for creating and editing reminders with datetime-local input and note textarea, converting between bigint nanoseconds and JS dates.",
  "frontend/src/components/SolarProjectFormDialog.tsx": "Dialog form for creating and editing solar projects with customer selection, system size (kW), installation status, and site survey section (date, surveyor, notes).",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page with stat cards for leads/customers/deals/revenue, a deal pipeline bar chart, solar project status pie chart, and quick stats row — fixes LeadStatus enum comparison.",
  "frontend/src/components/Layout.tsx": "Main authenticated layout with sidebar navigation; uses `isSuperAdminEmail()` (checking `vcrm_logged_in_email` from localStorage) as an additional admin check so the super admin bypasses the approval gate, and conditionally shows the Admin Panel sidebar link for admin/super-admin users.",
  "frontend/src/pages/LoginPage.tsx": "Two-step OTP login page that stores both authentication state (`vcrm_otp_authenticated`) and the logged-in email (`vcrm_logged_in_email`) in localStorage after successful OTP verification, enabling the super admin email check in Layout.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions — restored `useGetCustomer`, `useGenerateOTP`, and `useVerifyOTP` hooks that were accidentally removed, plus all CRUD hooks for leads, customers, deals, reminders, solar projects, and admin/approval operations.",
  "frontend/src/index.css": "Global styles with OKLCH color system — updated from deep-blue to emerald green and dark teal palette. Primary color is now emerald green (~155° hue), sidebar uses dark teal (~175° hue), orange accent highlights are preserved, and both light and dark modes use the new green/teal tokens.",
  "frontend/tailwind.config.js": "Tailwind configuration — updated color tokens to replace navy/blue-mid with teal/green-mid matching the new emerald green and dark teal theme. Box shadow colors updated to match the green palette. All other configuration unchanged.",
  "frontend/src/components/ProfileSetupModal.tsx": "Profile setup modal for new users with full client-side validation and error categorization; fixed to check actor readiness without requiring Internet Identity — uses OTP-based auth so only `!!actor && !actorFetching` is needed to enable the submit button.",
  "frontend/src/pages/ProfilePage.tsx": "Professional profile page at /profile displaying user name, email, phone, role, approval status, and principal ID in a card-based layout with inline editing for name/email/phone, read-only role/status fields, and a Re-verify via OTP modal dialog with countdown timer and OTP input.",
  "frontend/src/App.tsx": "Main app router with TanStack Router — uses a named layout route with `id: 'layout'` so child routes register as `/customers/$id` and `/solar-projects/$id` (not `/layout/customers/$id`), fixing the `useParams` route ID mismatch; includes all routes including `/admin`.",
  "frontend/src/components/RoleAssignmentOTPDialog.tsx": "New dialog component for OTP-gated role assignment — presents a two-step flow (email input → 6-digit OTP with countdown timer and resend option), verifies OTP via backend, then calls `requestRoleAssignment` on success; shows appropriate error toasts on failure and matches the existing OTP UI patterns.",
  "frontend/src/pages/CustomerDetailPage.tsx": "Customer detail page showing contact info, location map, and associated solar projects — fixed `useParams` `from` path to `/layout/customers/$id` matching the actual TanStack Router route ID generated by the named layout route."
}